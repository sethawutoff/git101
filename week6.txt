Pass by value คือการก็อปปี้ค่าของตัวแปรนึงไปให้อีกตัวแปรนึงในฟังก์ชัน แล้วใช้ค่าที่ก็อปปี้ได้แค่ในฟังก์ชันเท่านั้น
Pass by reference คือการใช้ * ในการชี้บอกโปรแกรมให้ส่งค่าของตัวแปรนี้เข้าไปทำงานในฟังก์ชันแล้วถ้ามีการเปลี่ยนแปลงก็จะเปลี่ยนใน main ด้วย ส่ง address แล้วรับเข้าฟังก์ชันด้วย pointer